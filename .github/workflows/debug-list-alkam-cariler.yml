name: Debug List ALKAM Cariler

on:
  push:
    branches:
      - debug/list-alkam-cariler-20260519

jobs:
  list-cariler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: List ALKAM cariler
        run: node tools/list-alkam-cariler.mjs
